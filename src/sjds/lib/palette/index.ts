// styels
import lightTheme from 'sjds/lib/palette/light';
import darkTheme from 'sjds/lib/palette/dark';
// types
import { ThemePalette } from 'sjds/types/Palette';

/** 테마 색상표 SET */
const themePalette: ThemePalette = {
  light: lightTheme,
  dark: darkTheme,
};

export default themePalette;
